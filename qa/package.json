{
  "name": "innosabi-qa-challenge",
  "version": "1.0.0",
  "description": "innosabi QA engineer take-home challenge — Playwright + TypeScript against saucedemo.com",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:flaky": "playwright test tests/checkout.spec.ts -g \"completes a checkout with one item\" --repeat-each=10 --workers=1",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}